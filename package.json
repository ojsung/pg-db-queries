{
  "name": "database-queries",
  "version": "1.0.0",
  "description": "Export a set of popular queries.  Should only be used in cases where the columns or number of values cannot be hardcoded.  It will perform slower than hardcoded queries, as it accounts for dynamic number of columns and values.",
  "main": "db-manager.js",
  "scripts": {
    "test": "test"
  },
  "author": "O. Jacob Sung",
  "license": "MIT",
  "dependencies": {
    "pg": "7.12.1"
  },
  "devDependencies": {
    "@types/node": "12.7.11",
    "@types/pg": "7.11.0"
  }
}
